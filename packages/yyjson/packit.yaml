upstream_project_url: https://github.com/ibireme/yyjson

jobs:
  - job: pull_from_upstream
    trigger: release
    dist_git_branches:
      - rawhide

  - job: koji_build
    trigger: commit
    dist_git_branches:
      - rawhide

  - job: bodhi_update
    trigger: commit
    dist_git_branches:
      - rawhide
